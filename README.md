# qs-cdn

Public asset bucket for Quietly Smart. Files here get a stable HTTPS URL via GitHub Pages and auto-deploy on push. Use for images that need a public URL but no web page (e.g. HTML email assets).

Not secret - public by design. Do not store anything sensitive.

- `email/` - newsletter template images (logo, header wave)
